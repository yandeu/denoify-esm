{
  "name": "denoify-esm",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "start": "tsc && node dist/index.js && denoify && deno run deno_dist/index.ts"
  },
  "devDependencies": {
    "@types/node": "^16.4.13",
    "denoify": "^0.7.2",
    "typescript": "^4.3.5"
  }
}